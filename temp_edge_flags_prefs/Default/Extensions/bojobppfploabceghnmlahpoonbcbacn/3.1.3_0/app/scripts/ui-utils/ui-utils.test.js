import{ellipsisSlice}from"./ui-utils";describe("UI Utils",()=>{describe("ellipsisSlice",()=>{it("should truncate text longer than maxLength and add ellipsis",()=>{const t=ellipsisSlice("This is a very long text that needs truncating",10);expect(t).toBe("This is a ...")}),it("should use default maxLength of 22 if not specified",()=>{const t=ellipsisSlice("This is a fairly long text string that should be truncated");expect(t).toBe("This is a fairly long ...")}),it("should not modify text shorter than maxLength",()=>{const t=ellipsisSlice("Short text",20);expect(t).toBe("Short text...")})})});